import { OnDestroy } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { Location } from '@angular/common';
import { OverlayRef } from '@angular/cdk/overlay';
export declare class LightboxComponent implements OnDestroy {
    sanitizer: DomSanitizer;
    /** Gallery ref id */
    id: string;
    /** Overlay ref to close the lightbox */
    overlayRef: OverlayRef;
    /** close button svg data */
    closeIcon: string;
    /** Subscription to changes in the user's location. */
    private _locationChange$;
    /** Use slide animation on opening and closing the lightbox */
    slideAnimation: any;
    /** Dispose the overlay when exit animation is done */
    onMouseLeave(e: any): void;
    constructor(sanitizer: DomSanitizer, location: Location);
    ngOnDestroy(): void;
}
